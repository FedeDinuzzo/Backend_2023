// Versiones y paquetes

// Node version manager, cambia las versiones de node
// https://github.com/coreybutler/nvm-windows/releases
// nvm-setup.exe

// terminal: nvm --version 
// 1.1.11
// nvm list available

// Cambiar version node:
// nvm install 20.1.0 (example)
// nvm use 20.1.0
// node -v (20.1.0)

// nvm list (las que tenes instaladas y en uso)

// Desintalar (primero salir de la rama y luego eliminarla)
// nvm use 19.8.1
// nvm uninstall 20.1.0


// YARN desarrollado por meta
// Yarn es mas rapido y seguro

// Instalar YARN
// npm i -g yarn
// yarn --version
// yarn init -y
// genera un package.json mas chico
// yarn add
// yarn remove


// Aspectos de npm vistos
// npm init -y
// npm run
// npm uninstall
// npm update
// npm outdate

// Aspectos de npm faltantes
// npm ci
// Seria clear install, si el paquete esta en ambos lo instalo, si solo esta en un package.json no lo instala
// npm audit
// ver los mensajes (green, moderate (yellow), high(red))
// npm audit fix --force

// package-lock.json
// Aparecen en orden como se instalaron las versiones
// Podemos bloquear las actualizaciones de las dependencias



// CREACION MODULO NPM

// Registro en npm
// npmjs.com -> signup
// Mas de 50 mil millones de descargas semanales

// npm init --y
// Readm.md (Hola, este es mi primer paquete de node.js)
// npm install npm@latest -g (actualizar npm en la pc)
// npm login (nos hace loggearnos Two-Factor Authetication)
// npm publish (subir a npm)

// Ya podemos instalar el paquete